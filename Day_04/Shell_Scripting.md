# shell 
>is simply a macro processor that executes commands. The term macro processor means functionality where text and symbols are expanded to create larger expressions.

> Unix shell is both a command interpreter and a programming language. 

>The Bourne shell is the traditional Unix shell originally written by Stephen Bourne. 